namespace Dataedo.App.Tools;

public class DependenciesTooltips
{
	public const string Uses = "Hierarchy of objects used by this object imported from the source (not all sources provide this information). You can also drag & drop objects from repository navigator (left pane) to define dependencies manually. ";

	public const string UsedBy = "Hierarchy of objects that use this object imported from the source (not all sources provide this information). You can also drag & drop objects from repository navigator (left pane) to define dependencies manually.";
}
